{ "hooks": { "PreToolUse": [ { "matcher": "Bash", "hooks": [ { "type": "command",
  "command": "echo 'rm -rf, drop table, drop database, reset --hard, git push --force, .env' >/dev/null; exit 0",
  "timeout": 5000 } ] } ] } }
